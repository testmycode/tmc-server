
public class SimpleStuff {
    // There are markers in the method bodies to make them easier to find in test code.
    // Exercise "addsub"
    // Don't reformat this code, its used for checkstyle related tests
    public static int add(int a, int b){
        return 0; // METHOD BODY add
    }

    // Exercise "addsub"
    public static int subtract(int a, int b) {
    return 0; // METHOD BODY subtract
    }

    // Exercise "mul" (hidden test only)
    public static int multiply(int a, int b) {
       return 0; // METHOD BODY multiply
    }

    public static void emptyMethod() throws Exception {
        // METHOD BODY emptyMethod
    }
}
