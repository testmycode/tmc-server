<% if not signed_in? %>
  <%= form_for :session, :url => sessions_path, :html => {class: "navbar-form pull-right"} do |f| %>
    <%= f.text_field :login, class: 'span2' %>
    <%= f.password_field :password, class: 'span2' %>
    <%= f.submit "Sign in", class: "btn btn-primary" %>
  <% end %>
<% end %>
