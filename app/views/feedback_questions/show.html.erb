<h1>Feedback question for <%= @course.name %></h1>

<p class="warning">Note: editing the question will NOT delete existing answers.</p>

<%= render :partial => 'feedback_questions/form', :locals => { :question => @question } %>

<div class="link-back"><%= link_to 'Back', :back %></div>
